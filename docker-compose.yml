version: '3.8'

services:
  scylla:
    image: scylladb/scylla:latest
    container_name: scylla
    ports:
      - "9042:9042"
    command: --smp 1 --memory 1G
    volumes:
      - scylla-data:/var/lib/scylla

volumes:
  scylla-data:
